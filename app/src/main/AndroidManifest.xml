<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.project">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="대외활동 어플"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Project">
        <activity android:name=".event.EventListActivity"></activity>
        <activity android:name=".event.EventItemActivity"></activity>
        <activity android:name=".event.WebViewActivity"></activity>
        <activity android:name=".event.Stopwatch"></activity>
        <activity android:name=".event.Calendar"></activity>
        <activity android:name=".Menu"></activity>

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>